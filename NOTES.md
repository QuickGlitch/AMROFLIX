## Composition

The code uses atomic design inspired composition. A clear example of this would be the layout component leveraging smaller components to render one larger composition.

Similary smaller components can leverage others, for example the button component uses the icon and typography components internally.

## Design system

core tokens, theme tokens and component tokens

## preventing spam

debouncing + caching

## singleton

theming
